# ── Web runtime ───────────────────────────────────────────────────────────────
# This is what Vercel installs for the serverless function. Keep it small: the
# bundle has a hard size limit and every package here is paid for on each cold
# start. Scraping / LLM / Google SDKs live in requirements-pipeline.txt and are
# imported lazily, so the web app runs fine without them.

fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.7.0
pydantic-settings==2.3.0

# HTTP — also the Supabase PostgREST transport and the Tavily client
httpx==0.27.0

# Templating for the dashboard and the public enforcement tracker
jinja2==3.1.4

# Structured logging (never stdlib logging)
structlog==24.2.0

# FastAPI form / file uploads
python-multipart==0.0.9

# .env loading
python-dotenv==1.0.1

# In-process scheduler (long-running deployments only; Vercel uses Vercel Cron)
apscheduler==3.10.4
